import { css } from '@emotion/react';
import color from '@/styles/color';
import typo from '@/styles/typo';

export const tagStyle = css(typo.Main.Medium, {
  color: color.MAIN,
  padding: '10px 13.5px',
});
