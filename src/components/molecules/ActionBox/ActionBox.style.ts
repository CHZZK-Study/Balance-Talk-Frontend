import { css } from '@emotion/react';
import color from '@/styles/color';

export const actionBoxContainer = css({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  backgroundColor: color.WT,
  gap: '6px',
});
