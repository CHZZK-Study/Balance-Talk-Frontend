import { css } from '@emotion/react';

export const UserUtilitySectionWrapper = css({
  display: 'flex',
  alignItems: 'center',
  gap: '1rem',
});

export const UtilityButtonsWrapper = css({
  display: 'flex',
  height: '38px',
  gap: '1rem',
});

export const UtilityButtonWrapper = css({
  display: 'flex',
  alignItems: 'center',
});
